name: first workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: print greeting
        run: echo "hello world"
      - name: print bye
        run: echo "good bye"
      - name: print job done
        run:  |
            echo "we are done"
            echo "finally job completed"
        
    
